import 'package:flutter/material.dart';

class Reportpage extends StatelessWidget {
  const Reportpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("No Reports currently!")));
  }
}
